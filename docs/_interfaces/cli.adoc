---
layout: default
title: CLI
nav_order: 2
parent: Interfaces
permalink: /interfaces/cli/
has_children: true
---
= CLI

Uniword ships with a command-line interface built on Thor. Invoke it with the
`uniword` executable after installing the gem. The CLI surfaces nearly every
Microsoft Word feature as a one-shot command -- see
link:../../guides/word-for-the-command-line/[Word for the command line] for the
side-by-side mapping.

== Global options

All commands accept `--verbose` (`-v`) for detailed output.

== Commands

=== Document operations

|===
| Command | Description

| `uniword convert INPUT OUTPUT`
| Convert a document between DOCX, MHTML, and HTML

| `uniword info FILE`
| Display document statistics and structure

| `uniword validate FILE`
| In-memory model-level validation, prints rule-coded issues

| `uniword verify FILE`
| Three-layer DOCX verification (OPC, XSD, semantic)

| `uniword check FILE`
| Run accessibility and quality checks

| `uniword repair INPUT OUTPUT`
| Load, reconcile, and save a repaired copy with a per-fix report

| `uniword build TEMPLATE OUTPUT`
| Build document from a `.docx` template

| `uniword batch PATTERN OUTPUT_DIR`
| Batch convert documents

| `uniword metadata FILE`
| Display or edit document metadata

| `uniword generate INPUT OUTPUT`
| Generate DOCX from structured text (YAML/Markdown)

| `uniword version`
| Show the installed Uniword version
|===

=== Document feature subcommands

|===
| Command | Word equivalent

| `uniword theme ...`
| Design tab (Themes, Colors, Fonts)

| `uniword styleset ...`
| Styles pane, Style Set gallery

| `uniword styles ...`
| Styles pane -- list, remove, rename

| `uniword fonts replace INPUT OUTPUT`
| Home → Replace → Replace Fonts

| `uniword page setup INPUT OUTPUT`
| Layout → Page Setup dialog

| `uniword toc ...`
| References → Table of Contents

| `uniword headers ...`
| Insert → Header & Footer

| `uniword images ...`
| Insert → Pictures

| `uniword watermark ...`
| Design → Watermark

| `uniword review ...`
| Review tab (comments and tracked changes)

| `uniword diff ...`
| Review → Compare

| `uniword spellcheck check FILE`
| Review → Spelling & Grammar

| `uniword protect ...`
| Review → Restrict Editing

| `uniword template ...`
| File → New from template

| `uniword resources ...`
| Export Word resources from a local install
|===

== Topics

* link:../convert-command/[convert] -- Format conversion
* link:../info-command/[info] -- Document inspection
* link:../validate-command/[validate] -- Structural validation
* link:../verify-command/[verify] -- Three-layer verification pipeline
* link:../check-command/[check] -- Quality and accessibility checks
* link:../repair-command/[repair] -- Repair and reconciliation
* link:../build-command/[build] -- Template building
* link:../batch-command/[batch] -- Batch processing
* link:../metadata-command/[metadata] -- Document metadata
* link:../generate-command/[generate] -- DOCX generation from structured text
* link:../review-command/[review] -- Comments and tracked changes
* link:../diff-command/[diff] -- Document comparison
* link:../spellcheck-command/[spellcheck] -- Spell and grammar checking
* link:../images-command/[images] -- Image management
* link:../toc-command/[toc] -- Table of Contents
* link:../headers-command/[headers] -- Headers and footers
* link:../watermark-command/[watermark] -- Watermarks
* link:../protect-command/[protect] -- Document protection
* link:../theme-commands/[theme] -- Theme operations (apply, fonts, colors, auto)
* link:../styleset-commands/[styleset] -- StyleSet operations (list, apply, extract)
* link:../styles-commands/[styles] -- Styles pane (list, remove, rename)
* link:../fonts-command/[fonts] -- Replace Fonts
* link:../page-command/[page] -- Page Setup dialog
