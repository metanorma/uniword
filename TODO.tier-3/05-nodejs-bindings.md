# 05 — Node.js bindings

**Status:** PLANNED (Tier 3)
**Priority:** Audience expansion
**Depends on:** nothing

## Why

Node dominates enterprise tooling. JS/TS is the most common
language for internal tools.

## Approach

Same as Python: HTTP client wrapping the HTTP API (Tier 3/02).
Distributed as `@metanorma/uniword` npm package.

## Why Tier 3

Same as Python: separate repo, depends on HTTP API.
